{
  "name": "agent-guidance-dashboard",
  "version": "1.0.5",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test || echo 'Playwright not installed — skipping e2e'"
  },
  "devDependencies": {
    "vitest": "^2.1.0",
    "@playwright/test": "^1.48.0"
  }
}
